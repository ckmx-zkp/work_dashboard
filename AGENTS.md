# AGENTS.md — work_dashboard（跨仓协作看板）

> 本仓是跨仓状态同步点，不存业务实现。进入本仓的 AI 会话仍须先读 `D:/Home_Work/AGENTS.md`，其中规则优先于本文件。

## 开工

1. 先执行 `git status --short`。仅工作树干净时执行 `git pull --ff-only`；不干净时不得 pull、不得覆盖或混入他人改动。
2. 阅读 `AI-Pet项目全景与进度.md`（代码核实快照）和 `AI-Pet协作看板.md`（日常状态真源）；固件任务再读 `AI-Pet固件联调看板.md`。
3. 看板只写状态、事实、依赖和待决项。接口 schema、设计细节与密钥不得写入本仓。

## 修改与提交

- 仅在代码、接口、部署、正式需求决策或跨仓依赖状态发生变化时更新看板；纯分析、调研、未确认方案不写入。
- 追加进度日志时使用“日期 + 仓库/侧 + 事项”。跨仓接口变化须注明“契约已变更”，并以各仓契约文档为准。
- 提交前复查 `git status`，只 `git add -- <目标文件>`；确认未混入他人改动后再提交和推送。
- 若同一文件有并行改动或无法安全快进，停止写入并协调，不强行合并或覆盖。
