# AI Pet 固件 ↔ 服务端联调看板

> 用途：固件开发会话（ESP32）与 xiaozhi-server 会话之间的联调状态同步点。
> **使用规则（所有 AI 会话必读）：**
> 1. 开工前先读本文件，了解对侧当前状态。
> 2. 完成一个任务后立即更新对应状态和文末"进度日志"（一行：日期 + 侧 + 事项）。
> 3. 本文件只记**状态与事实**；协议细节以官方协议文档和 `xiaozhi-server/docs/02-部署OTA与设备接入.md`、`xiaozhi-server/docs/04-设备MCP与外设路由.md` 为准。
> 4. 涉及业务侧的协作（persona、记忆、绑定）以 `AI-Pet协作看板.md`（本目录）为准，本文件不重复维护。
> 5. 密钥、密码不进本文件，只记位置。

## 仓库/侧 地图

| 侧 | 路径 | 职责 | 当前开发会话 |
|----|------|------|-------------|
| 固件 | `D:\Home_Work\ESP32_XIAOZHI\xiaozhi-esp32` | ESP32 固件（小智官方固件二开）：配网、语音、MCP Server（眼睛等外设） | 固件会话 |
| 服务端 | `D:\Home_Work\xiaozhi-server` | 自建小智兼容后台：OTA/激活、实时语音、MCP 路由 | 会话 B（Kimi） |

## 服务端接入信息（固件侧必读）

| 项 | 值 |
|----|-----|
| 服务器 | 阿里云北京 ECS `39.107.143.71`（8C16G） |
| **OTA 地址（固件要改的）** | `http://39.107.143.71:8002/xiaozhi/ota/`（改 `CONFIG_OTA_URL` 或 NVS `ota_url`） |
| WebSocket 会话地址 | `ws://39.107.143.71:8000/xiaozhi/v1/`（OTA 应答自动下发，固件无需硬编码） |
| 智控台 | `http://39.107.143.71:8002`（设备激活码在这里输入绑定） |
| 部署版本 | 上游 v0.9.6 全模块（server + manager + MySQL + Redis） |
| 当前模型链路 | ASR=豆包流式 2.0 / LLM=MiniMax-M2.5（备用 千帆/GLM-4.5-Flash/Kimi K2.7）/ TTS=火山双向流式·湾湾小何（备用 EdgeTTS）；**以 AI-Pet协作看板.md 模型链路行为准** |
| 备用接入 | 旧测试机 47.108.114.17 上有一个停用的精简模式容器（已废弃，勿用） |

## 联调集成点状态（双方共同维护）

状态值约定：`未开始 / 进行中 / 已联通 / 已验收`

| # | 集成点 | 说明 | 固件侧 | 服务端侧 | 联调 |
|---|--------|------|--------|----------|------|
| 1 | OTA 指向自建服务 | 固件改 ota_url → 拿到激活码应答 | ✅ 已联通（真机请求 `39.107.143.71:8002`） | ✅ OTA 可达 | ✅ 已联通 |
| 2 | 设备激活绑定 | 板子显示 6 位码 → 智控台输入完成绑定 | ✅ 已联通（码 `965423`，`Activation successful`） | ✅ 已就绪 | ✅ 已联通 |
| 3 | WS 会话建立 | 连接 8000，hello/auth 握手 | ✅ 已联通（板子重启重取 OTA 后连接成功） | ✅ 已修复（`server.websocket` 参数补公网地址） | ✅ 已联通 |
| 4 | 首轮语音对话 | 唤醒 → ASR → LLM → TTS 全链路 | ✅ 已联通（ASR 识别"你是谁呀？"→ GLM 人设回复 → TTS 播报，眼睛联动 happy） | ✅ GLM-4.5-Flash + FunASR + EdgeTTS 全链路工作 | ✅ 已联通 |
| 5 | MCP 眼睛工具 | `self.eye.look/blink/close/open/set_emotion` 路由 | 固件已注册五工具，已有 emotion 等历史真机证据 | 服务端已部署全量路由与 b12 批量后处理 | 进行中：待五项完整真机回归 |
| 6 | 打断/停止 | 语音打断 TTS | 进行中：设备端 AEC + realtime 已增量编译通过，待烧录真机 | 服务端已有 abort / turn ID 作废链 | 进行中 |
| 7 | 断网重连恢复 | 按上游能力验证 | 未开始 | 未开始 | 未开始 |
| 8 | （V0.3+）灯带/舵机/行为控制器 | 固件新增 MCP 工具，服务端只路由 | 未开始 | 未开始 | 未开始 |
| 9 | 外设状态旁路上报 | 成功眼睛工具快照经服务端 → backend | 固件工具已提供状态动作 | 服务端 b12 已部署单项/批量快照上报；backend 接口已上线 | 进行中：待真机落库证据 |
| 10 | BLE 偶遇双 AI 交流（E9） | 匿名低速 BLE 发现 → 主人同意 → 换短期 token → 双方上报 → backend 生成内容 → 受控播报结束 | 固件负责 BLE/同意/token 状态机；当前零实现 | backend 生成内容；xiaozhi 仅转发；不是实时会话桥 | **协议阻塞：BLE 包、双边同意、空闲控制通道待冻结** |
| 11 | E11 空闲设备主动播报 | backend 指定内容触发空闲设备按需建立语音 WS 并播放 | 架构稿已提交，建议独立 MQTTS 控制客户端；尚无代码 | backend docs/13 当前仅定义“语音 WS 在线时轮询”，xiaozhi 尚无实现 | **架构阻塞：两种在线前提未统一** |
| 12 | 多板型语音基线 | P4、BOX-3B、LCD EV Board 分板配置与自建 OTA | `faaae15` 已补配置/平台类型；LCD EV V1.5 已构建烧录并进入 speaking/listening | 服务端沿用同一 OTA/WS 链路 | 进行中：待 LCD 480×480 屏与“你好小鹿”目视/唤醒验收 |

> 已知问题（2026-08-01）：①误唤醒率偏高——板端 AFE/WakeNet 灵敏度问题，**固件侧**调阈值；服务端只能减弱「误触后的体感」（见下方服务端待执行）；②「休息/闭眼」后会话仍停留在 listening，唱歌也会被 ASR 接住——产品缺口，固件可挂断会话，服务端可做人设/退出编排（见下方）；③日志告警 `server 16000 vs device 24000` 重采样；④TTS 口音已修（EdgeTTS → `zh-TW-HsiaoChenNeural`）。

## 服务端待执行（会话 B / 智控台）

> 以下由**服务端侧**落地；固件侧不改这些配置。完成一项请改状态并记进度日志。

| # | 事项 | 操作位置 / 说明 | 优先级 | 状态 |
|---|------|-----------------|--------|------|
| S1 | 临时关闭开场唤醒回复 | 智控台参数已设 `enable_greeting=false`，随下一次连接生效。 | 高 | 已部署，待真机验收 |
| S2 | 「休息/睡觉」时顺带结束会话 | `v0.9.6-b5`：仅在“休息/睡觉/晚安”等请求成功调用 `self_eye_close` 后，取消旧 LLM 后续输出→播一句“晚安，先休息啦。”→WS 断开；普通“闭眼”不退出。 | 高 | 已部署，待真机验收 |
| S3 | 调整无语音断连窗口 | `v0.9.6-b13`：智控台参数由 30 秒调为 300 秒，支持短暂停顿后继续对话；明确休息仍由 S2 立即断开。 | 中 | 已部署，待真机验收 |
| S4 | 对齐下行音频采样率 | 智控台 `xiaozhi.audio_params.sample_rate` 已由 16000 调为 24000，与设备输出对齐。 | 中 | 已部署，待真机验收 |
| S5 | 复核 MCP 眼睛工具全量路由 | 已核对 `look/blink/close/open/set_emotion` 均按设备上报名称原样经 MCP 路由；`v0.9.6-b5` 只在成功后维护/回传状态，不改设备协议。2026-08-18 的批量 `function_calls` 逐项快照回传与休息断开已随 `v0.9.6-b12` 部署，容器级定向验收通过。 | 中 | 已部署，待真机回归 |
| S6 | （非服务端）误唤醒阈值 | WakeNet 阈值 / 麦增益 → **固件侧**，服务端勿当作 ASR/LLM bug 排查。 | — | 固件跟 |
| S7 | b8 思考隔离 + C5 + 身份行真机回归 | `v0.9.6-b8` 上线（2026-08-16，服务端镜像切换，无需刷固件）。用户已真机确认：① 唤醒/对话不播报 think 内容；② 首轮回复体现 C5 动态上下文；③ 能正确承认星座与 MBTI 身份。 | 高 | 已验收 |
| S8 | 会话空闲趣味续聊提醒 | 建议以最后有效用户语音计时，约 90 秒轻提醒、240 秒最后确认、300 秒退出；不得打断 ASR/LLM/TTS/MCP。首版可由服务端固定短句实现，个性化内容待 backend 契约。 | 中 | 待开发 |

**不归服务端、避免误派工：**
- 开机数秒内 `Wake word detected` 且无人说话 → 端侧误唤醒
- `EspTcp: TCP receive failed: -1` 后立刻重连成功 → 断线竞态噪音，先观察
- 眼睛 `closed=1` 在再次唤醒后仍闭着 → 固件/工具未自动 `open`，非 WS 故障

## device_id 对齐约定

- 固件上报 MAC（`Device-Id` 头）→ 小智侧 device_id 即 MAC 格式
- P4 本板 MAC：`8c:fd:49:0c:a8:78`；UUID：`e21a5f7d-47f5-4729-b3d9-029d8e7d6aee`
- BOX-3B 与 LCD EV Board 均是独立设备，需各自配网/激活，不沿用 P4 历史；LCD EV V1.5 已记录独立 MAC 并跑通语音状态
- 业务 backend 侧 `device_uid` 与 MAC 对齐（表已建唯一索引，见 AI-Pet协作看板）
- 曾绑定小智官方云的板子：需先在官方控制台解绑或重新激活（eFuse/激活态）

## 当前进度摘要

### 固件侧（固件会话维护）


- ✅ `CONFIG_OTA_URL` 已切自建并烧录；真机 WiFi `TP-LINK_C738` / IP `192.168.0.100`
- ✅ OTA + 激活 `965423` + WS 对话 + 退出意图均已真机验证
- 🟡 **设备端 AEC + realtime 打断已增量编译通过**：AI Pet 板型进入 `USE_DEVICE_AEC` 白名单，`CONFIG_USE_DEVICE_AEC=y`；待烧录后验收 TTS 中直接开口打断，空闲双击 BOOT 可开关 AEC。
- 🟡 **并行板型 ESP32-S3-BOX-3B**（2026-08-18）：官方 `esp-box-3` + 默认 320×240 对话屏 + 设备端 AEC + 自建 OTA；不搬眼睛/外设。全量编译通过；睡觉/待机因无 `self.eye.close` 不能走现有 S2，需服务端按意图独立断开。
- 🟡 **ESP32-S3 LCD EV Board V1.5**（2026-08-19）：IDF 5.5.2 构建并烧录 COM15，GC9503 480×480；串口已见 speaking/listening。待复位补完整启动日志、屏幕目视和“你好小鹿”唤醒验收。
- ⚠ P4 端侧误唤醒偏高；realtime 可能叠加误打断，需真机一起观察。P4 CMake 引用的 9 个状态图 `.bin` 未入 Git，干净克隆需先生成。

### 服务端侧（会话 B 维护）


- ✅ v0.9.6 全模块；OTA/WS/激活/首轮对话真机已通；`server.websocket` 已为公网 IP
- ✅ 模型链路可用；退出命令 `退出` / `handle_exit_intent` 真机有效
- 🟡 S1–S4、S5 服务端路由已随 `v0.9.6-b5` 部署；待真机回归验收
- ✅ **语音容器已切 `xiaozhi-aipet-server:v0.9.6-b13`**：含 b8 思考隔离/C5、b10 Memory MCP、b11 设备自动导入、b12 FIFO/多工具后处理与 b13 300 秒会话窗口。
- 📋 **BLE 偶遇双 AI 交流需求已校正**：固件负责匿名低速 BLE 发现、主动询问主人、双边同意及同意后短期 token 交换；双方上报后由 backend 生成本次受控内容，xiaozhi 只转发。当前零实现。

## 待决事项

| 事项 | 阻塞谁 | 状态 |
|------|--------|------|
| 服务端 S1–S5（开场白 / 休息即退出 / 超时 / 采样率 / MCP 路由） | 服务端 | 已部署，待真机验收；S7 已验收 |
| 固件：设备端 AEC + realtime 开口打断真机验收（误打断/误唤醒一并观察） | 固件 | P4 已编译，待烧录验收 |
| 固件：LCD EV Board V1.5 屏幕/唤醒收口 | 固件 | 已构建烧录且语音状态运行；待 480×480 目视、“你好小鹿”与完整启动日志 |
| BOX-3B/LCD EV “睡觉即断开” | xiaozhi-server | 无眼睛工具，现有 S2 依赖 `self.eye.close`；可直接开发意图成功后的独立断开 |
| 固件：WakeNet 阈值；可选 `self.eye.close` 后挂断会话 | 固件 | 待用户拍板 |
| 域名+ICP 备案（上 TLS/WSS、MQTT 8883 前提） | 双方 | 未开始 |
| BLE 偶遇双 AI 交流：匿名发现包、双边同意、token TTL/重放、空闲控制通道、内容段数/时长/离线收尾 | 固件 / xiaozhi / backend / 产品 | **跨仓协议阻塞，当前零实现** |
| E11 主动播报传输：已有语音 WS 轮询，还是独立 MQTTS/控制 WS 唤醒空闲设备 | backend / xiaozhi / 固件 / 运维 | 待用户/架构拍板；未统一前不开发 |

## 进度日志

| 日期 | 侧 | 事项 |
|------|-----|------|
| 2026-08-01 | 服务端 | 全模块上线；OTA/WS/智控台三端口公网验证通过；默认模型链路配齐（GLM 默认 + Kimi 备用 + 本地 ASR + EdgeTTS） |
| 2026-08-01 | 固件 | `CONFIG_OTA_URL` 已切自建；复测 OTA POST 有激活码，但下发 WS 仍为占位域名 `xiaozhi.server.com`（DNS 失败），已标阻塞 |
| 2026-08-01 | 固件 | 真机联调：OTA/激活 `965423` 成功；唤醒后仍连 `ws://xiaozhi.server.com:8000` 失败（DNS）；#1/#2 已联通，#3 阻塞 |
| 2026-08-01 | 服务端 | 修复 OTA 下发 WS 占位域名（`server.websocket` 参数置空导致 manager 用兜底值）；另修复 `server.auth_key` 与 `server.secret` 不一致隐患；模拟设备 hello+MCP 握手已通过；待板子重启重取 OTA |
| 2026-08-01 | 固件 | 真机确认 #3/#4 已联通；补充「服务端待执行」S1–S5（greeting/休息退出/超时/采样率/MCP），供会话 B 执行 |
| 2026-08-02 | 服务端 | `v0.9.6-b5` 部署：关闭唤醒问候、无语音断连设 30 秒、下行 24 kHz；“休息/睡觉”成功闭眼后短确认并主动断开，取消旧 LLM 尾音；五个眼睛 MCP 工具仍原样路由。待真机回归。 |
| 2026-08-16 | 服务端 | 语音容器切 `v0.9.6-b8`（b6 直跳 b8，开机即生效、无需刷固件）：跨 chunk `<think>` 状态机过滤（MiniMax-M2.5 思考内容不再进 TTS）+ C5 动态上下文合入 Prompt + backend persona_pack 身份行（问星座应承认天蝎座）；新增 S7 真机回归三项，S1–S5 回归项不变。 |
| 2026-08-18 | 服务端 | 批量眼睛 MCP 调用成功后的逐项外设快照回传及 `self_eye_close` 休息断开已随 `xiaozhi-aipet-server:v0.9.6-b12` 部署；容器级定向验收通过，待并入 S5/X1 真机回归。 |
| 2026-08-18 | 真机 | 用户确认 S7 三项通过：MiniMax `think` 不进入 TTS、首轮回复包含 C5 动态上下文、设备正确承认星座与 MBTI 身份。 |
| 2026-08-18 | 项目看板复核 | 校正联调状态：五个眼睛 MCP 工具与外设状态旁路均非“未开始”；固件工具已存在，服务端 b12 与 backend 接口已部署，当前统一记为待完整真机回归/落库证据。 |
| 2026-08-18 | 项目看板 / 架构复核 | 固件仓新增未提交 `AI_PET主动唤醒与主动播报协作看板_zh.md`：明确空闲时语音 WS 已关闭，建议独立 MQTTS 控制面触发按需 WS。该前提与 backend docs/13“连接建立/连接中轮询 pending”不一致；新增联调项 #11 并标架构阻塞，待传输方案和契约统一。 |
| 2026-08-17 | 固件 | 设备端 AEC + realtime 打断已增量编译通过：AI Pet 板型加入 `USE_DEVICE_AEC` 白名单，`CONFIG_USE_DEVICE_AEC=y`；待烧录后验收 TTS 中直接开口打断和误打断。 |
| 2026-08-18 | 服务端 / 产品 | 双机对聊房间（方案 B）实时桥归 xiaozhi-server，backend 不做实时桥，固件几乎不改；统一为联调项 #10，待拍板配对入口和单轮句数，不插队 X1。 |
| 2026-08-18 | 服务端 | `v0.9.6-b13` 已部署：无语音会话窗口从 30 秒延长至 300 秒，运行容器回读配置为 300；8002 HTTP 与 8000 TCP 验证通过。趣味续聊提醒登记为 S8，尚未实现。 |
| 2026-08-18 | 固件 | 工作树切到 ESP32-S3-BOX-3B（官方 `esp-box-3`）：默认对话屏 + 语音 + 设备端 AEC + 自建 OTA；不搬眼睛/外设。S3 全量编译通过。P4 sdkconfig 已备份。待 BOX-3B 烧录、配网、重新激活。 |
| 2026-08-19 | 固件 / 项目看板复核 | 固件 `main=faaae15`：ESP32-S3 LCD EV Board V1.5 已用 IDF 5.5.2 构建并烧录，串口进入 speaking/listening；待 480×480 屏、“你好小鹿”与完整启动日志验收。同步登记多板型配置/平台类型、无眼睛板型 S2 断开缺口及 P4 眼睛资产复现风险。 |
| 2026-08-19 | 固件 / backend / xiaozhi / 产品 | **双 AI 交流需求校正，旧方案作废**：不再做 App/智控台/口头口令配对或两路语音 WS 实时桥。改为户外低速 BLE 匿名发现，机器人主动询问主人；双方同意后交换短期 token 并各自上报，backend 生成本次双方受控交流内容。联调项 #10 已重写；当前零实现，先冻结 BLE 包、双边同意与空闲控制通道。 |
